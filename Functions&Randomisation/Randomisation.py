#randomisation is used where you need the random numbers, you can get random integers using random.randint(paras) and also floating point numbers using random.random(no paras) which has range 0 to 1 exclusive and can get your required range by multiplying it to the 10.

import random
mynum=random.randint(0,1)
if mynum==0:
    print("Heads")
else:
    print("Tails")
import modules
number=modules.number
print(number)
